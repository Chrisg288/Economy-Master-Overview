import {escapeHtml,findTreeNode,downloadJson} from './utils.js';
export function createInspector({container,exportButton,trees,products,store}){let current=null,currentRecord=null,token=0;function row(label,value){return `<tr><td>${escapeHtml(label)}</td><td>${escapeHtml(value??'')}</td></tr>`}async function render(state){const myToken=++token,tree=trees[state.sector]||trees.home;current=findTreeNode(tree,state.selectedNodeBySector[state.sector]||tree.id)?.node||tree;currentRecord=null;if(current.product_record_id)currentRecord=await products.recordForNode(current);if(myToken!==token)return;const productRows=currentRecord?[row('Record ID',currentRecord.record_id),row('Need Path',currentRecord.need_path),row(currentRecord.record_kind==='service'?'Service':'Product',currentRecord.item_name),row('Manufacturer',currentRecord.manufacturer||'—'),row('Model / Part',currentRecord.model_part_number||'—'),row('Source',currentRecord.source_vendor),row('Source Record',currentRecord.source_record_id),row('Status',currentRecord.record_status),row('Availability',currentRecord.availability),row('Risk',currentRecord.risk),row('Risk Type',currentRecord.risk_type),row('Updated / Year',currentRecord.last_updated||currentRecord.catalog_year),row('Historical Cost',currentRecord.total_cost==null?'—':`${currentRecord.total_cost} ${currentRecord.currency}`),row('Evidence',currentRecord.evidence)].join(''):'';container.innerHTML=`<div class="inspector-title"><span class="status-badge">${escapeHtml((current.type||'object').toUpperCase())}</span><h3>${escapeHtml(current.label)}</h3><span class="status-badge">${escapeHtml(state.sector)}</span><span class="status-badge">${escapeHtml(state.tool)}</span></div><div class="inspector-actions"><button class="compact-button">NEW</button><button class="compact-button">OPEN</button><button class="compact-button">EDIT</button><button class="compact-button">SAVE</button></div><div class="inspector-actions"><button class="compact-button">SIMULATE</button><button class="compact-button">ASSESS</button><button class="compact-button">DOCUMENT</button><button class="compact-button">COMPARE</button></div><div class="module-note">Tree and datagrid selections are linked. Record status identifies current, historical, reference and safety-evidence records.</div><h4>IDENTITY</h4><table class="property-table">${row('Name',current.label)}${row('Class',current.type||'')}${row('Sector',state.sector)}${row('Scope',state.scope)}${row('Overlay',state.overlay)}${row('Tool',state.tool)}${row('Priority',current.priority_rank||'—')}${row('Linked Records',(current.product_count||0).toLocaleString())}${row('Description',current.description||'')}${productRows}</table>`}exportButton.addEventListener('click',()=>current&&downloadJson('selected-economy-object.json',currentRecord?{tree_node:current,product_record:currentRecord}:current));store.subscribe(render);render(store.get())}
