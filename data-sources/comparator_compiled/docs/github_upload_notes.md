# GitHub Upload Notes

This ZIP is GitHub-ready.

Upload the included folders to the root of:

```text
Economy-Master-Overview
```

It should create or update:

```text
data-sources/comparator_compiled/
docs/layout/
```

Do not move these files into the repository root.

Recommended root stays clean:

```text
index.html
README.md
assets/
data/
data-sources/
docs/
Business-Simulator/
Comparator-definitions/
Prototypes/
```

The large raw PDFs/catalogs should not be copied into normal GitHub Pages unless intentionally archived elsewhere. Keep raw catalogs local or use GitHub Releases for large archive assets.
