# Economy Master UI Patch v0.3.3

Upload this patch after the v0.3.2 micro-batches. It contains only interface/runtime files; it does not duplicate the large product corpus.

Main behavior: single-line sortable datagrid, horizontal scrolling, flyout inspector, and clearer product/service leaf labels.
