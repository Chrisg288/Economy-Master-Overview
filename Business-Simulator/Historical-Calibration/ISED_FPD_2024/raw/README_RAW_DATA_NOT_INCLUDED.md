# Raw ISED CSV archive not included in GitHub-safe package

The original raw archive was intentionally removed from this GitHub browser-upload package because it is larger than GitHub's 25 MB web-upload limit.

Removed file:

```text
ISED_Financial_Performance_Data_2024_Raw_CSV.zip
```

Keep the original archive on your local drive.

The repository still contains:

- normalized calibration profiles;
- lookup tables;
- source catalogs;
- summaries;
- validation records;
- schemas;
- processing scripts;
- source/method documentation.

For full reproducibility, retain the raw CSV archive locally or publish it separately as:

- a GitHub Release asset;
- Git LFS content;
- another data archive;
- or files downloaded again from ISED using the included source information.
