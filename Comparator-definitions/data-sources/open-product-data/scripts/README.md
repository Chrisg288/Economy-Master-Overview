# Download Scripts

The PowerShell and Python scripts download a curated core set of product-reference datasets. They continue after individual download failures so one source outage does not stop the full run.

The scripts intentionally do not bulk-download every Health Canada API table or the very large Open Food Facts dump unless requested.
